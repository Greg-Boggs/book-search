# Astro dev server, started with the project.
web_extra_daemons:
  - name: astro
    command: "cd /var/www/html && npm run dev -- --host 0.0.0.0 --port 4321"
    directory: /var/www/html

web_extra_exposed_ports:
  - name: astro
    container_port: 4321
    http_port: 4320
    https_port: 4321
